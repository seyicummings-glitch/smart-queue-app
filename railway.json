{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python backend/manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "python backend/manage.py migrate --noinput && gunicorn --chdir backend --bind 0.0.0.0:${PORT:-8080} --workers 1 --timeout 120 sqms.wsgi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
